setcps(140/60/4)

register('acidenv', (x, pat) => pat.lpf(100)
        .lpenv(x * 9).lps(.2).lpd(.12)
)

$: s("bd*4")
  .duck("3:4")
  .duckdepth(.8)
  .o(2)
  .decay("<.5>")
  .fm(2)
  .sustain(0)._scope()


$:  s("[sd:8]*2")
  .decay(.1)
  .delay(1)
  .lpf(slider(900,0,900))
  .scope()._spectrum()
$: n(irand(24))
  .sound("saw")
  .room(2)
  .diode("<.1 .5>")
  .acidenv(slider(0.758))
  .decay(0.5).seg(16)
  .lpf(2000)._scope()

_$: s("[ride:2]*16").decay(0.05)
  .gain(0.3)
  ._scope()

$: s("pulse")
  .scale("e3:major")
  .vowel("a e i o u i")
  .delay(1)
  .delayfb(.5)
  .delaysync(1).orbit(2)
  .seg(16).lpf(600)
  ._scope()

$: n(irand(32)).sound("gm_fx_echoes")
  .decay(.5)
  .sus(4)
  .seg(16)
  .lpf(900)
  .acidenv(slider(0.042))._spectrum()


$: s("[wt_digital:5,sawooth]!16")
  .wt(.4)
  .wtenv(.4)
  .acidenv(slider(0.614))
  .o(2)
  ._scope()

$: s("tanpura").slow(2.1)
  .acidenv(0.5)
  ._spectrum()
